import AboutUs from "@/components/AboutUs";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Trapezi | About Us",
};

function AboutUsPage() {
  return <AboutUs />;
}

export default AboutUsPage;
